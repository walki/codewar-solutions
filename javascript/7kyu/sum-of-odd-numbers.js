function rowSumOddNumbers(n) {
	
  /*
  let m = 0;
  let start = 1;
  while (m < n)
  {
    start += m*2;
    m++;
  }
  
  let sum = 0;
  for(var i = 0; i < n; i++)
  {
    sum += start + i*2;
  }
  return sum;
  */
  
  return n*n*n;
  
}