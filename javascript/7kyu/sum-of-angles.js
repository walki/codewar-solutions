function angle(n) {
    // The General Rule
    // Each time we add a side (triangle to quadrilateral, quadrilateral to pentagon, etc),
    // we add another 180° to the total:
    
    return (n-2) * 180;
  }